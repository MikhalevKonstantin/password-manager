import 'package:password_manager/logic/store/app_state.dart';
import 'package:velocity_x/velocity_x.dart';

class InitializeIAP extends VxMutation<AppState>{
  @override
  perform() async {

    // await store?.iapState.init();

    // FirebaseIAP.ready && products list ready

    // isSubscribed?
  }
}